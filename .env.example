# 🏛️ AFO Kingdom 환경변수 템플릿
# 이 파일을 복사해서 .env로 만들고 실제 값들을 채워주세요
# 절대 실제 시크릿 값을 커밋하지 마세요!

# ========================================
# 필수 시크릿 (시스템 작동에 반드시 필요)
# ========================================

# JWT 토큰 서명용 시크릿 키 (64바이트 hex 권장)
JWT_SECRET_KEY=your_jwt_secret_key_here

# PostgreSQL 데이터베이스 비밀번호
POSTGRES_PASSWORD=your_postgres_password_here

# SSL 인증서 암호화용 패스프레이즈
AFO_PASSPHRASE=your_ssl_passphrase_here

# ========================================
# 선택 시크릿 (서비스별 필요에 따라)
# ========================================

# OpenAI API 키 (GPT 사용 시)
# OPENAI_API_KEY=sk-proj-your_openai_key_here

# Anthropic Claude API 키 (Claude 사용 시)
# ANTHROPIC_API_KEY=sk-ant-your_anthropic_key_here

# Ollama 서버 URL (로컬 LLM 사용 시)
# OLLAMA_BASE_URL=http://localhost:11435

# N8N 워크플로우 서버 URL
# N8N_URL=https://your-n8n-server.com

# ========================================
# 모니터링 및 알림 (선택)
# ========================================

# Prometheus Pushgateway URL (CI 메트릭 전송용)
# PUSHGATEWAY_URL=https://your-pushgateway.com

# Pushgateway 기본 인증 (username:password 형식)
# PUSHGATEWAY_BASIC_AUTH=your_credentials_here

# ========================================
# 개발 환경 설정
# ========================================

# Python 환경 (production에서는 제거)
PYTHONPATH=/Users/brnestrm/AFO_Kingdom/packages/afo-core

# Node.js 환경 (선택)
# NODE_ENV=development

# ========================================
# 주의사항
# ========================================
#
# 1. 이 파일(.env.example)은 템플릿입니다. 실제 값들은 .env에 넣으세요.
# 2. .env 파일은 절대 Git에 커밋하지 마세요 (.gitignore에 이미 제외됨).
# 3. 실제 시크릿 값들은 서비스 대시보드나 openssl rand로 생성하세요.
# 4. 시크릿 회전 시 이 템플릿도 함께 업데이트하세요.
# 5. 보안 프로토콜: docs/SECURITY_PROTOCOL.md 참고
#
# 생성: 2026-01-09
# 버전: 1.0.0