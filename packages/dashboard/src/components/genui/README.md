# GenUI Sandbox (Phase 9)

This directory contains React components autonomously generated by **Samahwi (AFO GenUI Service)**.

- **Warning**: Code here is AI-generated.
- **Protocol**: Verify via Playwright before promoting to main components.
- **Structure**: Each component matches its PascalCase name (e.g. `MagicButton.tsx`).
