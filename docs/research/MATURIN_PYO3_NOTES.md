# Maturin/PyO3 연구 노트

> **⚠️ NOT_IN_SCOPE (Do not implement until Ticket5+)**  
> **격리 사유**: Ticket2는 이미 Pydantic Contract Gate로 안정화됨. Rust/PyO3 도입은 범위 폭발 위험.

**작성일**: 2025-12-23  
**상태**: 연구 노트 (실행 문서 아님)

---

## 📋 격리 사유

1. **Ticket2는 "Pydantic Contract Gate(검증)"로 이미 안정화** 됨
   - Rust/PyO3/Maturin 도입 시 범위 폭발 위험
   - Ticket3 가기도 전에 툴체인 전쟁 발생 가능

2. **실제로는 AFO에서 검증된 사실이 하나도 없음**
   - maturin 설치/빌드/휠/CI 크로스컴파일 검증 없음
   - 현재는 "연구 노트"이지 "티켓/SSOT"가 아님

---

## 🔍 기술적 가능성 (참고용)

### Rust 설치 여부
- **Rust**: 설치됨 (rustc 1.91.1)
- **Cargo**: 설치됨 (cargo 1.91.1)

### PyO3 설치 여부
- **PyO3**: 미설치 (필요 시 설치 가능)

### 프로젝트 구조
- **Rust 파일 (.rs)**: 없음
- **Cargo.toml**: 없음
- **현재 빌드 시스템**: setuptools (pyproject.toml)

---

## ⚖️ 도입 시 고려사항

### 장점
1. **성능**: 대용량 HTML 파싱 시 Rust가 더 빠를 수 있음
2. **타입 안전성**: Rust의 강력한 타입 시스템
3. **메모리 안전성**: 메모리 누수 방지

### 단점
1. **복잡도 증가**: 빌드 시스템 복잡도 증가 (Maturin, Rust 툴체인)
2. **개발 속도**: Rust 학습 곡선, 디버깅 어려움
3. **배포 복잡도**: 크로스 컴파일, 플랫폼별 바이너리 관리
4. **의존성 증가**: Rust 툴체인, Maturin, PyO3 등
5. **현재 필요성 없음**: 35개 위젯 파싱은 Node.js로 충분

---

## 🎯 도입 조건

### 현재 (Ticket 2)
- ❌ **도입 불필요**
- ✅ **Node.js 파서 유지** (이미 완료, 검증됨)
- ✅ **Pydantic Contract Gate** (백엔드/CI에서만 사용)

### 미래 (Ticket 5+ 또는 위젯 1000개 이상)
- **조건**: 위젯 개수가 1000개 이상이고, 파싱 시간이 1초 이상일 때
- **방법**: Maturin + PyO3로 Rust 확장 모듈 생성
- **통합**: Python 패키지로 빌드하여 `packages/afo-core`에 통합

---

## 📋 결론

### 현재 상태
- **Rust/PyO3 사용 가능**: 기술적으로 가능하지만 **현재 불필요**
- **현재 구현**: Node.js 파서로 충분 (35개 위젯, 밀리초 단위)
- **권장**: 현재 구조 유지

### 미래 고려
- 위젯 개수가 1000개 이상이고 성능 이슈가 발생할 때만 고려
- 그때는 Maturin + PyO3로 Rust 확장 모듈 도입 검토

---

**상태**: 연구 노트 (격리됨). Ticket 5+ 또는 위젯 1000개 이상 시 재검토.

